import { hero, defineColorHealth, colorHero } from './src/js/app.js' 

test('check the color of health', () => {
  // Функция проверки
  // 1. Выполняем нужные нам действия
  TODO:
  // 2. Проверяем результат с помощью:
  expect(<что получили>).toBe(<что должно быть>);
  })